{$DOMAIN} {
	encode zstd gzip

	handle_path /api/* {
		reverse_proxy app:8787 {
			# SSE (/api/events): отдаём поток без буферизации
			flush_interval -1
		}
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
